# Forex Factory News Scraper Service

เว็บเซอร์วิสที่ scrape ข่าวเศรษฐกิจจาก Forex Factory และให้บริการผ่าน API

## คุณสมบัติ

- 🔄 Scrape ข้อมูลข่าวเศรษฐกิจอัตโนมัติทุกวันอาทิตย์เวลา 00:00 (เขตเวลาไทย)
- 📊 กรองเฉพาะข่าว High Impact สำหรับสกุลเงิน USD, EUR, GBP, AUD, NZD
- 🌐 เสิร์ฟข้อมูลผ่าน REST API
- ☁️ Deploy บน Railway.app ได้ง่าย

## API Endpoints

- `GET /` - หน้าแรก ข้อมูล API
- `GET /api/news` - ดึงข้อมูลข่าวในรูปแบบ JSON พร้อม metadata
- `GET /weekly_ecocar.json` - ดาวน์โหลดไฟล์ JSON โดยตรง
- `GET /health` - ตรวจสอบสถานะเซอร์วิส
- `GET /scrape-now` - บังคับ scrape ทันที (สำหรับทดสอบ)

## วิธี Deploy บน Railway

### ⚠️ สำคัญ: Railway ต้องการ Dockerfile

Railway จะใช้ Dockerfile ที่มีอยู่แล้วในโปรเจ็คนี้ เพื่อติดตั้ง Playwright และ dependencies ทั้งหมดอย่างถูกต้อง

### ขั้นตอน Deploy:

1. **สร้าง Git repository และ push โค้ด:**
```bash
git add .
git commit -m "Add Dockerfile and configurations"
git push origin main
```

2. **Deploy บน Railway:**
   - ไปที่ [Railway.app](https://railway.app)
   - คลิก "New Project" → "Deploy from GitHub repo"
   - เลือก repository ของคุณ
   - Railway จะตรวจพบ Dockerfile และ build อัตโนมัติ
   - รอประมาณ 3-5 นาที สำหรับ build และติดตั้ง Chromium

3. **ตรวจสอบการทำงาน:**
   - เปิด URL ที่ Railway ให้มา เช่น `https://your-app.railway.app`
   - ทดสอบ endpoint: `/health` เพื่อดูสถานะ
   - ทดสอบ scrape: `/scrape-now` (ใช้เวลาประมาณ 2-3 นาที)
   - ดูข้อมูล: `/api/news` หรือ `/weekly_ecocar.json`

4. **ตั้งค่า Environment Variables (ถ้าต้องการ):**
   - ใน Railway dashboard → Variables
   - สามารถเพิ่ม `PORT` หรือตัวแปรอื่นๆ ได้

### 🐛 Troubleshooting

**ถ้า scrape ไม่ทำงาน:**
1. ตรวจสอบ logs ใน Railway dashboard
2. ดูว่า Chromium ติดตั้งสำเร็จหรือไม่
3. ลอง redeploy โดย push commit ใหม่

**ถ้าได้ data = []:**
- อาจเป็นเพราะ Playwright ยังไม่ได้ติดตั้ง
- ดู logs ว่ามี error "Executable doesn't exist" หรือไม่
- Dockerfile จะแก้ปัญหานี้อัตโนมัติ

## ตัวอย่างการใช้งาน

```bash
# ดึงข้อมูลข่าว
curl https://your-app.railway.app/api/news

# ดาวน์โหลดไฟล์ JSON
curl https://your-app.railway.app/weekly_ecocar.json

# บังคับ scrape ใหม่
curl https://your-app.railway.app/scrape-now
```

## ทดสอบ Local

```bash
# สร้าง virtual environment
python -m venv env
source env/bin/activate  # หรือ env\Scripts\activate บน Windows

# ติดตั้ง dependencies
pip install -r requirements.txt

# ติดตั้ง Playwright browsers
python -m playwright install chromium

# รันเซอร์วิส
python app.py
```

จากนั้นเปิดเบราว์เซอร์ไปที่ http://localhost:5000

## หมายเหตุ

- ไฟล์ `weekly_ecocar.json` จะถูกสร้างและอัปเดตอัตโนมัติทุกวันอาทิตย์
- เซอร์วิสจะ scrape ครั้งแรกทันทีเมื่อ start up หากยังไม่มีไฟล์ข้อมูล
- ใช้เวลา scrape ประมาณ 2-3 นาที ขึ้นอยู่กับความเร็วอินเทอร์เน็ต
